# Copy to .env and fill in your values before running the server.

PORT=3000

# MongoDB Atlas connection string (Phase 2)
MONGODB_URI=mongodb+srv://<user>:<password>@cluster0.xxxxx.mongodb.net/neurostep?retryWrites=true&w=majority
